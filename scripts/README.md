# scripts

| Script | Purpose |
| --- | --- |
| `setup.sh` | One-command local setup: virtualenv, backend install, frontend build, `.env`. `--minimal` skips torch. |
| `capture-screenshots.py` | Screenshot the running app with Playwright, for the README. |

Both are idempotent and safe to re-run.
